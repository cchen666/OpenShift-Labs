spec:
  identityProviders:
    - ldap:
        attributes:
          email:
          - mail
          id:
          - sAMAccountName
          name:
          - cn
          preferredUsername:
          - sAMAccountName
        bindDN: cn=binduser2,ou=技术支持,ou=support,dc=titamu,dc=com
        bindPassword:
          name: ldap-secret
        insecure: true
        url: ldap://10.0.169.17/ou=openshift,ou=support,dc=titamu,dc=com?sAMAccountName?sub?(&(objectclass=user))
      mappingMethod: lookup
      name: ldapidp
      type: LDAP
    - htpasswd:
        fileData:
          name: htpass-secret
      mappingMethod: claim
      name: my_htpasswd_provider
      type: HTPasswd